<% if user_signed_in? %>
	<div class="panel panel-default col-xs-3">
			<div class="panel-body col-xs-12">
				<div class="">
					<%= link_to dictions_path do %>
					<%= attachment_image_tag current_user, :image, format: "png", fallback: "img/acount_image1.png", :class => "acount_image center-block" %>
					<% end %>
					<h3 class="text-center font-45"><%= current_user.name %></h3>
					<h4 class="user_word text-center"><%= current_user.user_word %></h4>
				</div>

				<div class="list-group">
					<a href="" class="list-group-item" data-toggle="modal" data-target="#edit_user" data-backdrop="static"><span class="glyphicon glyphicon-user" %></span>　アカウント情報の編集</a>
					<a href="" class="list-group-item"></a>
					<a href="" class="list-group-item">お問い合わせ</a>
					<%= link_to "ログアウト", destroy_user_session_path, method: :delete, :class => "list-group-item" %>
				</div>
			</div>
	</div>
	<%= render partial: "layouts/edituser" %>

<% end %>